# Supabase backend

This folder will hold, as later milestones need them:

- `migrations/` — versioned SQL schema changes (Milestone 2 onward).
- `functions/` — Edge Functions for operations that need server-side secrets,
  such as calling the LLM provider (Milestone 5).

Nothing here yet — Milestone 1 only wires the client SDK in `shared/supabase`.
